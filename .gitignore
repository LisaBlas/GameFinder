# Dependencies
node_modules/

# Local venv for the pre-commit framework (lint tooling itself is Node/eslint)
.venv/

# UX review persona's local Playwright venv + scratch workspace
# (company/employees/gamefinder_ux.yaml) — isolated from the app's own npm
# deps, not part of the production build
.ux_review_venv/
.ux_review_scratch/

# Production build
dist/
build/

# Environment variables
.env
.env.local
.env.development
.env.production

# IDE and editor files
.vscode/
.idea/
*.swp
*.swo

# OS files
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Optional npm cache directory
.npm

# Optional eslint cache
.eslintcache

server/public
vite.config.ts.*
*.tar.gz